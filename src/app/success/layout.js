import React, { Suspense } from "react";

export const metadata = {
  title: "Order Success - British Furniture Suppliers",
  description: "Your order has been placed successfully",
};

const layout = ({ children }) => {
  return <>{children}</>;
};

export default layout;
